# This is a convenience symlink to the actual docker-compose file
# The actual file is located at: scripts/docker-compose.yml
# 
# To use: docker-compose -f scripts/docker-compose.yml up -d
#
# This file exists for backward compatibility but we recommend
# using the file in the scripts directory directly.

version: '3.8'
services:
  placeholder:
    image: alpine:latest
    command: echo "Please use 'docker-compose -f scripts/docker-compose.yml up -d' instead"